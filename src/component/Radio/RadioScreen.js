import React from 'react';
import { Text, View } from 'react-native';

const RadioScreen = () => {
    console.log("Inside RadioScreen");

    return (
        <View style={{justifyContent: 'center',alignItems: 'center',flex: 1}}>
            <Text>Radio</Text>
        </View>
    )
}

export default RadioScreen;